# IndWM Showcase

Static, GitHub Pages-ready showcase for the IndWM smart factory environment.

The deployable site is in `dist/`. The included workflow publishes that folder
to GitHub Pages after Pages is enabled with **GitHub Actions** as its source.
